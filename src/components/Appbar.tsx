import React from 'react';
import { Toolbar } from 'react-native-material-ui';

export default function Appbar(): React.ReactElement {
    return (
        <Toolbar 
            centerElement="Translator"    
        />
    );
}
